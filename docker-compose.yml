version: '3.8'

services:
  chromadb:
    image: chromadb/chroma:latest
    container_name: enterprise-chroma-vector-store
    ports:
      - "8000:8000"
    volumes:
      - chroma_data:/chroma/chroma
    environment:
      - IS_PERSISTENT=TRUE

volumes:
  chroma_data:
    driver: local
    
